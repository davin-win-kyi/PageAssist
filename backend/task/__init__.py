"""Task side — everything grounded in ONE specific webpage's real DOM:
- representation.py : analyze a page into a task representation (/task-representations/{id})
- events.py         : client-detected page changes → silent widget regeneration
"""
